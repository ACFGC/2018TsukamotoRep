#ifndef CHPBAR
#define CHPBAR
/*
#include "CCharacter.h"
#include "CBox.h"
#include "CCollider.h"
class CHpBar :public CCharacter{
public:
	CVector mSize;
	float mMax;
	float mHp;
	float mColor[4];
	CCharacter mBar;
	CCharacter*mpTask;
	CBox mBox;
	CTriangle mT[2];
	void SetHpBar(CCharacter*task,
		const CVector&pos, const CVector&size,
		float max, float hp);
	void SetDiffuse(float r, float g, float b, float a);
	void Update();
	void Render();
};*/
#endif
